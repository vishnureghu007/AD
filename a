ACT_ARCHIVES = [":{}_library".format(act)
                for act in EXPORTED]

GROUP_WHOLEARCHIVE_LINKOPTS = sum([
    ["-Wl,--whole-archive",
     "$(location {})".format(lbl),
     "-Wl,--no-whole-archive"]
    for lbl in ACT_ARCHIVES
], [])
